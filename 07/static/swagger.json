{
  "swagger": "2.0",
  "info": {
    "title": "Task API",
    "version": "1.0.0"
  },
  "paths": {
    "/tasks": {
      "get": {
        "description": "Returns list of existing tasks",
        "produces": [
          "application/json"
        ],
        "responses": {
          "200": {
            "description": "Successful operation"
          }
        }
      },
      "post": {
        "description": "Creates a new task",
        "produces": [
          "application/json"
        ],
        "consumes": [
          "application/json"
        ],
        "parameters": [
            {
            "name": "task data",
            "in": "body",
            "description": "JSON data",
            "required": true,
            "schema": {
              "$ref": "#/definitions/TaskData"
            }
          }
        ],
        "responses":{
          "201": {
            "description": "Successful operation"
          },
          "400": {
            "description": "Invalid request data"
          }
        }
      }
    }
  },
  "definitions": {
    "TaskData": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        }
      }
    }
  }
}